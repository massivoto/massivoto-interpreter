/**
 * AI Provider Types
 *
 * Re-exported from @massivoto/kit where the contracts live.
 * Kept for backward compatibility with existing imports.
 */
export type {
  AiProvider,
  AiProviderName,
  TextRequest,
  TextResult,
  ImageRequest,
  ImageResult,
  ImageAnalysisRequest,
  ImageAnalysisResult,
} from '@massivoto/kit'
export { DEFAULT_AI_PROVIDER } from '@massivoto/kit'
